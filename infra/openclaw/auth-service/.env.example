# Mission Control Auth Service Configuration

# Email whitelist (comma-separated)
ALLOWED_EMAILS=komapc@gmail.com

# Redis configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Flask secret key (generate with: python3 -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=

# SendGrid email configuration (optional - codes logged to console if not set)
SENDGRID_API_KEY=
FROM_EMAIL=noreply@mission.daatan.com
