name: Deploy Factum Atlas

on:
  push:
    branches: [main]
    paths:
      - factum_atlas.html
      - oracle-test.html
      - duel.html

  # Allow manual trigger from GitHub UI
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Only one deploy at a time
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare Pages content
        run: |
          mkdir -p _site
          cp factum_atlas.html _site/index.html
          cp factum_atlas.html _site/factum_atlas.html
          cp oracle-test.html _site/oracle-test.html
          # Copy duel report if it exists
          if [ -f duel.html ]; then cp duel.html _site/duel.html; fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
